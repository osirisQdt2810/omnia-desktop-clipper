"""Omnia Desktop Clipper — a standalone cross-platform AnkiConnect capture app.

Press a global hotkey in any app to send the selected text (word + context) to a
running Anki via AnkiConnect, reusing the exact contract + tags the Omnia web
clipper uses so the add-on's integration gateway auto-generates the note.

This package is a *client* of Omnia-Anki, not part of the add-on itself.
"""

from __future__ import annotations

__version__ = "0.1.0"
