# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/

# Local user config / secrets (never commit)
config.json

# Virtualenvs (the README's throwaway build venv + any local dev venv)
.venv/
.venv-build/

# PyInstaller build output (produced per-OS by `python build.py`)
build/
dist/
*.spec
